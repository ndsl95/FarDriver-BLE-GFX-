# UI V2 说明

新界面专为 ES3C28P 的 240×320 竖屏设计，继续使用 TFT_eSPI 直绘，不改变 BLE、远驱协议、FT6336G 驱动和业务回调。

## 页面结构

- 扫描页：明确的 BLE 搜索状态、动态扫描指示和 200×44 大触控按钮。
- 设备页：最多显示 5 台设备，包含 RSSI 数值、信号条和清晰的选中状态。
- 仪表页：依据用户提供的赛车仪表参考图，使用 READY/SPORT/BT 顶栏、分段彩色环形刻度、中央大速度与档位、五项数据卡、电池及油门进度条；断开与详情操作固定在底部。
- 信息页：将实时数据整理成两列卡片，避免旧界面标签和值拥挤。

## 工程切换

`platformio.ini` 当前排除了旧的 `ui_gfx.cpp`，编译 `ui_v2.cpp`。公开 API 仍为 `ui_gfx.h`，所以主程序无需修改。

如需临时恢复旧 UI，从 `build_src_filter` 中移除 `-<ui_gfx.cpp>`，并加入 `-<ui_v2.cpp>`。

## 硬件约束

- 显示：ILI9341V，240×320，`tft.setRotation(0)`。
- 触摸：FT6336G，当前竖屏方向直接使用原始坐标。
- 触控目标：主要按钮宽度不小于 56 px、高度不小于 32 px。
- 更新方式：静态页面一次绘制，实时值仅在内容变化时局部刷新，避免全屏闪烁。
