# 触摸失灵问题调试记录与解决方案

> 项目：FarDriver BLE 仪表盘（ESP32-S3 + ILI9341V + FT6336G 触摸）
>
> 本文档记录了本项目中"触摸屏无响应 / 页面卡死"问题的完整排查过程与最终解决方案，可作为后续维护与排障参考。

---

## 1. 问题现象演进

| 阶段 | 现象 | 触发条件 |
|---|---|---|
| 1 | 长时间运行后触摸偶尔无反应 | 骑行一段时间后 |
| 2 | 按钮变白但页面不切换 | 点击 Connect / Info / Disconnect |
| 3 | 彻底没反应（坐标恒为 0,0） | 修改代码引入的 bug |
| 4 | 比之前更难点击（点击瞬间双点干扰） | 干扰伪点 + 手指并存 |
| 5 | 拧动转把后立即失灵 | 数据更新/电磁环境变化 |
| 6 | 失灵后无法自愈，只能重启设备 | 干扰后芯片状态异常 |
| 7 | 拧动转把后整个页面卡死 | I2C 总线被干扰后恢复机制缺陷 |

---

## 2. 诊断机制

### 2.1 诊断任务（diagTask，独立 Core，每 5 秒打印）

主循环卡死时诊断任务依然运行，可精确定位卡点：

```
[DIAG] loop=12561271 rate=150542/s heap=219228 st=2 pg=2 conn=1 nAgo=15
       uiAgo=0 lAgo=0 tAgo=4 tErr=0 tch=1 tcnt=2 txy=179,276 tw=0
       wmx=0 w5=255 pb=0 fc=0 dc=0
```

| 字段 | 含义 |
|---|---|
| `loop` / `rate` | 主循环计数 / 每秒执行次数（正常 >10 万/s，卡死时骤降） |
| `heap` | 可用堆内存 |
| `st` / `pg` | 连接状态 / 当前页面 |
| `nAgo` | 距上次 BLE 数据通知毫秒数（数据流看门狗） |
| `uiAgo` / `lAgo` / `tAgo` | UI 处理 / 主循环 / 触摸读取的停滞时间 |
| `tErr` | I2C 连续失败计数 |
| `tch` / `tcnt` | 是否有触摸 / 触摸点数（1=单点 2=双点） |
| `txy` | 最近一次触摸坐标 |
| `tw` / `wmx` / `w5` | 压力值 / buf[4] 峰值 / buf[5] 峰值 |
| `pb` / `fc` | 5 秒窗口内按钮按下次数 / 成功触发次数 |
| `dc` | 5 秒窗口内漂移伪点清除次数 |

### 2.2 FT6336G 寄存器要点（实测结论）

| 寄存器 | 实测结论 |
|---|---|
| buf[0..1] | X 坐标：`((buf[0]&0x0F)<<8) \| buf[1]` |
| buf[2..3] | Y 坐标：`((buf[2]&0x0F)<<8) \| buf[3]` |
| buf[4] | **保留寄存器，恒为 0**（芯片不提供压力值，早期误判为压力） |
| buf[5] | 触点有效标志（255 = 有效），**不是压力** |
| 触摸点 id | **不固定**（0/1），单点触摸时 id 也可能是 1 |

---

## 3. 根因分析（最终结论）

### 3.1 表面现象

拧动转把后（骑行中），触摸芯片报"漂移双点"（tcnt=2 且坐标持续跳变），伪点占满芯片 2 点容量，真实手指无法上报，导致：
- 按钮锁定在伪点坐标上 → 松开时伪点仍在 → fire 永不触发
- 点击无效但按钮会变白（状态机已锁定）

### 3.2 真正根因（经过实验排除后确认）

**I2C 总线受电磁干扰 → 原有恢复机制缺陷导致主循环卡死：**

```
拧转把（电机/动力线电磁干扰）→ I2C 总线通信失败
  ├─ 失败读取每次等待 100ms 超时 → 主循环被拖死
  ├─ 自恢复无冷却 → 反复执行 300ms 复位（复位风暴）
  └─ 诊断任务（Core 0）与主循环（Core 1）同时操作 Wire 总线
     → 跨核竞态互相打断 → 死循环
= 页面卡死 + 触摸永远不恢复（只能断电重启）
```

> 曾假设"SPI 高频写屏辐射干扰触摸芯片"，并通过分级节流（SPI 活动量降 1/3）验证 —— **无效**，该假设被证伪。

---

## 4. 解决方案（按层级）

### 4.1 状态机层（ui_gfx.cpp）—— 解决"点击无效"

| 方案 | 说明 |
|---|---|
| **选中点释放触发 fire** | fire 不再依赖"全局无触摸"（会被伪点掩盖），改为"选中的触摸点消失"即触发；用 `pressWasNew` 防误触发（只有新出现的手指点释放才 fire，常驻伪点消失不误触） |
| **漂移伪点过滤** | 跟踪锁定期间累计位移，真实点按位移极小（<20px），伪点持续漂移（>60px 阈值）→ 判定伪点立即解锁 |
| **多点选新点** | 手掌/伪点占用点 0 时，新出现的手指点（id 变化检测）优先选中 |
| **卡片选中计成功响应** | 列表页卡片按下即选中（不走 fire），选中时计入成功响应计数，使列表页同样享受失灵兜底检测 |
| **粘滞看门狗** | 按下状态持续 6 秒无松开 → 强制清状态并复位芯片（30 秒冷却防复位风暴） |

### 4.2 驱动层（ft6336g.cpp）—— 解决"页面卡死"（关键修复）

| 方案 | 原值 → 新值 | 作用 |
|---|---|---|
| I2C 读取超时 | 100ms → **10ms** | 失败快速返回，不拖死主循环 |
| 失败恢复阈值 | 10 次 → **3 次** | ~30ms 判定失败，尽早恢复 |
| 恢复冷却 | 无 → **5 秒** | 冷却期内跳过读取（触摸短暂静默），防复位风暴 |
| 跨核互斥 | 无 → **`_recovering` 标志** | recover 期间 getTouch 跳过读取，杜绝双核 Wire 竞态 |
| RST 复位等待 | 150ms → **50ms** | 复位阻塞缩短至 ~120ms |
| 垃圾数据过滤 | — | 无压力且坐标 (0,0) 的干扰帧丢弃，连续 20 帧触发自恢复 |

### 4.3 系统层（FarDriver_BLE_GFX.ino，diagTask 巡检）—— 兜底自愈

每 5 秒检查交互结果，检测到异常自动恢复：

| 检测 | 条件 | 动作 |
|---|---|---|
| 漂移风暴 | 5 秒内 ≥5 次漂移解锁 | 复位芯片 → 仍风暴则重启整机 |
| 隐形失灵 | 按了按钮 ≥2 次但 0 次成功响应（所有页面，含列表页卡片选中） | 复位芯片 → 仍无响应则重启整机 |
| 复位风暴 | 5 秒内 ≥3 次自恢复 | 重启整机 |
| 数据流看门狗 | 10 秒无 BLE 数据 | 强制重连 |

自动重启带 60 秒冷却，防止循环重启。**用户无需手动断电**。

---

## 5. 关键 Bug 复盘（容易踩的坑）

1. **单点 id=1 时坐标恒为 (0,0)**：多点选点逻辑中默认值 `x0=0` 初始化，当唯一触摸点 id=1 时选点落到无效默认值 → 表现为"彻底没反应"。修复：默认取第一个有效点 `pts[0]`。
2. **诊断计数被假 fire 污染**：空白处"选中点消失"也会走 fire 路径（fid=0 无操作）→ 计数虚增掩盖失灵检测。修复：仅 `fid>0` 才计数。
3. **`Wire` 库非线程安全**：两个 Core 同时调用 Wire API（诊断任务复位 + 主循环读取）会产生竞态。修复：`_recovering` 互斥标志。
4. **复位操作无冷却**：I2C 持续失败时每 ~100ms 触发一次 300ms 复位 → 主循环 75% 时间卡死。修复：统一 5 秒冷却 + 冷却期跳过读取。

---

## 6. 测试与验证

1. **正常点击**：任何页面点按应即时响应（按钮变白 → 松开触发）
2. **骑行场景（核心）**：连接后拧动转把，同时操作 Info / Back / Disconnect —— 数据更新和触摸应同时正常
3. **自动恢复**：串口出现 `[FT6336G] I2C fail` / `[WD] drift storm` 后，触摸应在 5 秒内自动恢复
4. **串口观察**：`dc=` 持续为 0 表示芯片干净；`pb`/`fc` 应同步增长（按了就触发）

---

## 7. 硬件建议（软件已到极限后的最终保险）

- 触摸排线（FPC）套磁环或贴屏蔽膜
- 触摸芯片 VDD 就近并联 10µF + 100nF 滤波电容（抑制电源纹波）
- 屏幕排线 / 触摸排线与动力线束分开走线
- 确认 FT6336G 的 RST 引脚确实连接到 MCU（早期版本曾与主控共享复位）

---

## 8. 相关文件

| 文件 | 职责 |
|---|---|
| `ui_gfx.cpp` / `ui_gfx.h` | UI 状态机、触摸交互、漂移过滤、fire 触发 |
| `ft6336g.cpp` / `ft6336g.h` | FT6336G 驱动、I2C 自恢复、恢复冷却与互斥 |
| `FarDriver_BLE_GFX.ino` | 主循环、diagTask 巡检、失灵检测与自动重启 |
| `config.h` | 触摸引脚、SPI 频率等硬件配置 |
